#!/bin/sh

set -x

# Unset Node debugger environment variables to prevent interference with git hooks
unset NODE_OPTIONS
unset VSCODE_INSPECTOR_OPTIONS

npm run --silent lint &&\
npm test --silent -- --silent &&\
npm run --silent build &&\
npm run --silent validate:build